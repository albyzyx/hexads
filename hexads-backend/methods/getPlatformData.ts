import InteractionModel from "../config/db/models/InteractionModel";

const getPlatformData = async ({ address }) => {
  const res = {
    totalAudience: 0,
    totalImpressions: 0,
    totalEngagement: 0,
    totalRevenue: 0,
  };
  return {};
};

export default getPlatformData;
